{
  "name": "resume-checker-root",
  "version": "1.0.0",
  "description": "Root proxy to run frontend easily",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "start": "cd frontend && npm run dev",
    "install-frontend": "cd frontend && npm install"
  }
}
